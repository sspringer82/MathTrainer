const operations = ['Addition', 'Subtraktion', 'Division', 'Multiplikation'];

export const operationsReducer = (state = { operations }, action) => {
  switch (action) {
    default:
      return state;
  }
};
